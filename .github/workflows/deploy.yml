name: Infosprint CI/CD Demo

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Display Success Message
        run: echo "CI/CD Pipeline Executed Successfully!"